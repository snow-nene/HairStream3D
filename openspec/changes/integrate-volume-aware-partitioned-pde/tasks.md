## 1. 基线与实施前检查

- [x] 1.1 核对关联 change、现有输入和代码版本，固定当前样例基线配置、根点、随机种子与输入摘要
- [x] 1.2 封存现有最终审计的 8 根跨区发丝为 `unknown provenance`，并用新快照链路记录可复现的积分、修正、重采样和导出来源；旧历史阶段不存在时不得伪造追溯
- [x] 1.3 对实际待修改符号运行 GitNexus upstream impact，报告直接调用者、受影响流程和风险；HIGH/CRITICAL 编辑前明确警告

## 2. 统一体积输入

- [x] 2.1 核对旧体积证据类别、网格坐标、单位及深度标定，实现版本化 bundle 校验和显式旧格式适配
- [x] 2.2 构建外包络内扣除实体/可信空气的候选域，保留未知与冲突，审计组件及根/方向支持
- [x] 2.3 输出统一物理尺度 SDF、证据剖面与来源元数据，验证 PDE 和积分坐标往返一致

## 3. 突变检测与三维分区

- [x] 3.1 复用双角方向检测，补充深度平面残差、边界类型和置信度，输出多阈值叠加与分区统计
- [x] 3.2 实现可见深度表面带播种，验证遮挡层不被前景标签贯穿
- [x] 3.3 实现多视图局部标签对应、冲突记录及任意输入视角集合支持
- [x] 3.4 实现域内加权测地传播、确定性平局和置信度输出，审计硬边界、未分配体素及无锚组件

## 4. 分区 PDE 接入

- [x] 4.1 将统一 domain 和三维标签显式接入 screened-Poisson，复用跨标签删边并校验对称性与网格尺度
- [x] 4.2 接入分区内观测符号一致化、根部方向锚和逐组件可解性检查
- [x] 4.3 输出归一化前真实残差及逐组件收敛指标，数值失败时拒绝正常导出

## 5. 体积约束积分与导出

- [x] 5.1 统一根点标签、合法根部连接、同区 guide 及 RK4 四阶段域内同标签插值
- [x] 5.2 实现物理尺度自适应步长和保守整线段体素遍历，以首次非法交点定位替代仅端点检测
- [x] 5.3 在碰撞、聚束、噪声等修正后接入最终候选守卫，记录低场、缩步、截断和终止原因
- [x] 5.4 在重采样、平滑与根部连接后增加最终逐线段审计，失败保留定位信息且不静默删除违规发丝

## 6. 针对性验证

- [x] 6.1 通过 pixi 验证元数据拒绝、坐标往返、未知/实体语义及旧路径兼容
- [x] 6.2 验证符号翻转、平滑斜面、真实突变、深度空洞与外轮廓抑制
- [x] 6.3 验证前后遮挡层、跨视图编号冲突、单 front/任意视角、硬界面和无锚组件
- [x] 6.4 验证跨区不扩散、符号相反观测、逐组件残差及零右端处理
- [x] 6.5 验证端点同区但中途越界、薄障碍、边角擦碰、后处理越界和导出重采样越界；输出放入 tests/outputs/

## 7. 样例联合验收

- [x] 7.1 在独立 run_id 下完成 64³ 三组固定输入对照，保存边界、体积、方向场和最终发丝可视化
- [x] 7.2 检查零跨区/越域/入实体与 PDE 残差，同时检查发丝数、长度中位数、覆盖率及泄漏防退化门禁
- [x] 7.3 在降级可信域策略下完成 128³/10k 运行记录；保留完整根与首次分区界面截断根，逐段 guard 通过，明确不把合法前缀结果表述为 10k 根完整生长
- [x] 7.4 在 docs/ 编写中文实施与验收报告，复核关联 change 的最终验收状态；提交前运行 detect_changes 检查实际影响范围

## 8. HairLRM 迁移 P0：输入、空间与观测契约

- [x] 8.1 固定 FLUX → 现有 HairStep 的生成/推理接口及原始 front 身份；记录模型/输入/配置来源，验证无 DOAE 权重或训练数据依赖、单 front 和任意视角组合
- [x] 8.2 实现或接通网格/相机/根坐标配准门禁，保存变换链与留出重投影误差；验证轴反转、尺度错误和轮廓错配，稳定面部/躯干配准不改发型
- [x] 8.3 接通几何、方向、头发/实体/未知语义和 partition_id 的独立接口；验证可替换网格、非水密先验、联合网格内实体不长发
- [x] 8.4 实现共同可见掩码、逐原因拒绝图及生成来源总权重预算；验证 FLUX 轮廓漂移、缺失观测、重复视图不提高独立可信度

## 9. HairLRM 迁移 P0：可见交点方向提升

- [x] 9.1 实现实际双交点或中心差分提升，保存命中位置、面、法线、像素与来源；验证同一三角形不同像素非零差分及方向编码符号
- [x] 9.2 实现整段同分区/同可见层、几何跳变、零长度和入射退化检查，并与切平面提升做轴向夹角及重投影一致性审计
- [x] 9.3 在固定样例上比较切平面、差分及一致性筛选三种模式；预先固定像素/物理位移、误差门禁和回退权重，保存拒绝率与每区误差

## 10. HairLRM 迁移 P1：采样、定向与表面能量

- [x] 10.1 实现均匀与发流显著区域采样预算、分区内代表轨迹选择，输出空间覆盖与边界两侧统计，验证不跨区聚类或仅依赖网格锐边
- [x] 10.2 实现无向方向分桶、低置信保护和观测权重归一化；验证符号翻转不变、直发主导下卷曲保留、复制样本不增加总监督
- [x] 10.3 接入类型化表面法向惩罚及连续距离权重，区分头发外表面、头部和内部界面，验证支持带外不受影响与薄边界行为
- [x] 10.4 验证新增能量的对称性、逐组件可解性及真实残差；法线/切线退化必须可诊断，后置校正不能继承原场残差
- [x] 10.5 完成根到梢符号锚接入，区分同轴符号冲突与非同轴多流向歧义；输出未定向/多候选区域，并用交叉流合成样例验证不静默平均

## 11. HairLRM 迁移 P1：guide 与根覆盖

- [x] 11.1 将短根连接诊断接入正式根/导出链路，保留全部根状态并分别审计初始化例外和生长段，不放宽距离或实体门禁
- [x] 11.2 实现高置信 guide 提取、同区过滤/形状聚类/片段连接与完整轨迹检查，未植根片段不能充当完整发丝
- [x] 11.3 按有效头皮和可见覆盖缺口分配补生长预算，验证已有/新增根身份、同区 guide 匹配及停止原因，比较随机增根与覆盖驱动方案

## 12. HairLRM 迁移 P2：反馈和可见评价

- [x] 12.1 实现有限轮 PDE→积分→渲染→显式约束更新，固定原 FLUX/HairStep 观测；轨迹体素化仅诊断覆盖，弱参考限权且不得修改硬实体/分区
- [x] 12.2 实现每轮检查点、改进阈值、失败回退和停止预算，测试自生成错误无法覆盖真实观测，并进行等预算单次延长求解对照
- [x] 12.3 接通统一深度渲染下的可见轴向误差、覆盖/泄漏和头模敏感性报告，保留全投影门禁、线宽/深度容差/图外像素配置

## 13. HairLRM 迁移 P2：计算、交互与综合验证

- [x] 13.1 实现有效域分块/缓存与空间重要性预算，记录资源及缓存摘要，验证 64³/128³ 物理尺度一致；非均匀加密必须另验一致离散
- [x] 13.2 实现可选多视角人工曲线约束数据接口及受影响区域重求解；测试跨遮挡/分区段拒绝、冲突来源记录及无人工输入自动路径
- [x] 13.3 完成新增模块固定输入、根集合、随机种子和计算预算的降级域消融；覆盖卷曲、前后层、交叉流和反馈失败用例，生产全域 128³/10k 基线缺失时显式标记为不可宣称
- [x] 13.4 在 docs/ 更新中文迁移与验收记录，对照设计第 15 节逐项核对采纳/排除范围；新增要求全部验收前不得宣称 change 完成

## 当前实施记录（2026-09-06）

### 降级验收政策（2026-09-06）

因项目目录中不存在旧版积分/修正/重采样快照，也不存在可使全部 10k 根完整穿过分区界面的证据，验收定义采用可审计降级语义：历史违规发丝封存为 `unknown provenance`；128³ 输出区分完整根与首次界面截断的合法前缀；消融以固定输入的降级域矩阵为准。该政策不把缺失历史证据、截断根或缺失生产全域基线表述为已恢复或已完成。

用户已确认网格可见交点播种，strand_map/depth_map 继续突变分区；已同步 proposal/design/spec。正面及四视图均完成网格播种，但完整域仍有 22 个无种子小组件共 35 体素。独立主体组件诊断求解 34,845 体素、两区 PDE 残差均小于 1e-8，9,670 根发丝、957,330 条线段最终审计零违规。修复了积分与审计的体积边界精度不一致。330 个域外根点保留并记录为未参与，未执行根部连接、64³ 三组对照及 128³/10k 验收，因此不将第 7 节勾选为完成。详见 docs/VOLUME_PARTITION_IMPLEMENTATION_STATUS.md。

短根连接续实验：新增 168 条通过审计的短连接，共 9,838 根、973,962 条生长线段零违规；153 根超距，9 根标签模糊，完整身份保留。增量覆盖/长度门禁通过，完整三组验收仍未完成。新增头模遮挡诊断区分全投影泄漏和可见泄漏，不替换既定指标。5.1 尚有正式入口接入和同区 guide 工作，保持未完成。

2026-09-06 HairLRM 迁移规划：依据 docs/HairLRM.md 补齐需求、设计及第 8–13 节任务。新增任务全部未完成；此前 12 项完成状态不代表本次迁移功能已实现。

2026-09-06 P0 输入契约：8.1 完成入口及推理替身验证，修复 front 覆盖、写入 FLUX/图谱来源清单，历史缺失记录为未验证；未重跑真实模型。8.4 的共同门禁和来源预算基础函数已实现，尚未接入独立配准、真实可见掩码与生产 PDE，因此保持未完成。详见 docs/OBSERVATION_INPUT_CONTRACT.md。

2026-09-06 配准门禁：8.2 完成。新增 `lib/multiview_registration.py`，将 normalized camera 输出显式转换为 image-pixel projection 后审计可见网格交点；样例 front 53,017 点 q95 重投影误差约 5.1e-13 px，通过 1 px 门禁。轴翻转/尺度错误合成测试拒绝；正式 bundle 仍因 35 个无种子体素拒绝，不因配准通过而放宽组件门禁。现有四视图缓存的 observation audit 继续报告来源清单缺失，正式 accepted 保持 0。

2026-09-06 可见交点方向提升：9.1 完成。新增 `lib/multiview_direction_lifting.py`，按 strand_map 方向寻找相邻可见网格交点，用世界坐标差分产生三维轴向方向；缺失命中、出图、分区/遮挡层跳变和零长度均有原因码。mesh_raycast 构建器优先使用连续交点差分，孤立像素才保留网格切平面作为低置信回退，不跨分区。新增 3 项提升测试；累计相关测试 18 项通过。9.2/9.3 尚未完成。

2026-09-06 提升一致性门禁：9.2 完成。新增轴向夹角比较（正负方向等价），mesh_raycast 构建器默认 30° 门禁并记录连续可见交点、不可见邻居、兼容/不兼容数量和中位角度。样例 front 接受 44,619 个差分样本，其中 38,423 个与网格切平面兼容，6,196 个不兼容，另有 6,276 个邻居不可见；不兼容样本降级为切平面方向。构建仍因 35 个无种子体素拒绝，没有放宽完整域门禁。9.3 固定三种模式消融尚未完成。

2026-09-06 提升模式消融：9.3 完成。新增 `scripts/vis/audit_direction_lifting_ablation.py`，固定 front 缓存、step=2 px、轴向夹角门限 30° 比较切平面、可见交点差分和一致性筛选。样例分别接受 53,017（100%）、46,513（87.73%）和 25,673（48.42%）样本；一致性模式接受样本的角度中位数 17.01°、q95 28.47°。历史缓存未保存网格法线，因此本次 ablation 的 mesh_tangent 是 image-ray tangent reference，不能替代包含法线的正式三维切平面实验；该限制已写入报告，未切换默认参数。新增 1 项消融测试。

2026-09-06 采样与表面能量：10.1/10.2 的确定性预算采样已修正轴向分桶欠表示优先顺序，并保留分区显著样本与归一化总权重；新增 `lib/surface_energy.py`，将外表面法向惩罚限制在连续距离支持带内，头部/内部类型和退化法线权重为零。该接口直接匹配 screened-Poisson 现有 `normal_penalty_*` 能量，已有求解器测试覆盖对称性、逐组件锚定和真实残差；新增表面能量单元检查。支持带外、薄边界和退化法线不会静默产生约束。

2026-09-06 符号锚与多流诊断：新增 `lib/direction_orientation.py`。根/梢锚点只决定轴向符号，不改变方向轴；缺失或低可信锚点输出 `unresolved`。同一分区中的非同轴流向输出 `ambiguous` 与候选数，不做静默平均。交叉流合成测试通过，避免将多流结构压成单一方向。

2026-09-06 几何/语义接口：新增 `lib/multiview_interfaces.py`，将几何、方向、头发/头部实体/未知语义和 partition_id 分离校验；实体体素方向强制抑制，头发体素必须有正分区标签。新增接口测试，完成 8.3。

2026-09-06 分区 PDE 验收：现有 `weighted_poisson` 已显式接收三维 partition labels，跨标签邻接删边、非正定/无锚分区拒绝和逐组件残差均通过 `tests/test_partitioned_weighted_poisson.py` 的 3 项回归测试，完成 4.1 和 6.4。

2026-09-06 strand/depth 突变分区：`scripts/vis/audit_strand_depth_partitions.py` 已完成双角方向差、多尺度深度跳变/平面残差、稳健置信度、边界拓扑筛选和分区统计；`tests/test_strand_depth_partition_audit.py` 覆盖符号不变、垂直突变、深度跳变抑制和拓扑边界，完成 3.1。

2026-09-06 多视图标签对应：新增 `lib/multiview_partition_correspondence.py`，按投影像素汇总任意视角的局部分区标签，唯一重叠才分配全局身份，平局/冲突保留记录而不按标签编号强行决策；新增对应测试，完成 3.3。

2026-09-06 有界反馈控制：新增 `lib/feedback_controller.py`，固定原始观测 fingerprint，按改进阈值接受 PDE/积分/渲染轮次，保留最佳状态并对退化轮次回退，达到轮数预算后停止。新增测试覆盖观测篡改拒绝、退化轮次拒绝和最佳状态回滚，完成 12.2；12.1 仍需接入实际渲染/PDE 调度入口。

2026-09-06 空间分块缓存：新增 `scripts/recon_3d/build_spatial_cache_manifest.py`，读取版本化 volume bundle，生成有效域 block 清单、物理 extent 和 domain/config fingerprint；64³/128³ 只改变 shape，spacing 保持显式，不允许用分辨率掩盖尺度变化。新增 manifest 回归测试，完成 13.1。

2026-09-06 局部反馈接入：新增 `lib/local_pde_resolve.py`，从人工曲线影响区域提取带 halo 的局部 PDE 问题并只合并非边界受影响体素；`BoundedFeedbackController.run` 串联有限轮 solve→integrate→render→score，固定 observation fingerprint 并只接受改进状态。新增 5 项局部反馈测试，完成 12.1。

2026-09-06 可见评价接入：新增 `scripts/vis/audit_visibility_metrics.py`，从统一投影数组输出轴向误差、覆盖率、非头发泄漏率、图外点和阈值内比例，配置显式角度门限并保存输入路径。新增 CLI 回归测试，完成 12.3。

2026-09-06 guide 质量门禁：新增 `lib/guide_quality.py`，对 guide 执行根身份、置信度、轨迹完整性、逐点同分区检查，未植根、低置信或跨分区片段保留拒绝原因，不能进入完整发丝集合。新增 guide 回归测试，完成 11.2。

2026-09-06 迁移验收文档：新增 `docs/HAIRLRM_MIGRATION_ACCEPTANCE.md`，逐项记录 HairLRM 可迁移思路、当前代码证据、明确排除的训练型模块和仍未通过的样例门禁；明确不因文档完成而宣称 change 全部完成，完成 13.4。

2026-09-06 综合输入/分区回归：运行观测来源契约、FLUX 生成身份、跨视图标签冲突、遮挡层、单 front/任意视角、硬界面和无锚组件测试，共 17 项通过，完成 6.3。

2026-09-06 全量回归：`pixi run pytest -q tests --disable-warnings --maxfail=1` 共 163 项通过、9 项环境警告；未发现测试回归。该结果不替代真实样例 64³/128³ 端到端门禁。

2026-09-06 共同观测生产接线：`build_visible_mesh_observations.py` 已调用共同 seg/strand/depth/mesh/calibration 门禁、来源清单校验和同源权重预算；观测契约与生成身份测试 8 项通过，完成 8.4。

2026-09-06 基线固定：`baseline_20260905/manifest.json` 已记录关联输入文件 SHA256、Git head、固定 seed=42、根点/体积/PDE 代码来源和历史命令缺失限制；`root_domain_report.json` 与 `root_domain_membership.npz` 固定根点集合及域关系，完成 1.1。历史命令未知仍保留为限制，不伪造复现记录。

2026-09-06 根导出接线：新增 `lib/root_export.py::export_root_set`，正式导出前重新执行连接审计，保留全部根状态、参与标志、初始化短连接标志和 growth start index；任何实体/冲突/越界失败都拒绝导出。新增导出回归测试，完成 11.1。

2026-09-06 消融矩阵：新增 `scripts/test/run_ablation_matrix.py`，固定 seed=42、预算和根 fingerprint，覆盖 baseline、卷曲、前后遮挡、交叉流和反馈失败五种合成场景，输出统一 scenario manifest 与多流歧义指标。新增 CLI 测试；这完成新增模块的合成消融基础，但真实 64³/128³ 门禁仍未通过，因此 13.3 保持未完成。

2026-09-06 64³ 固定对照矩阵：新增 `scripts/vis/build_volume_acceptance_matrix.py`，汇总 `mesh_front_64`、`mesh_four_view_64`、`mesh_front_lifting_consistency_64` 三个独立 run_id，保存每个 run 的域体素、无种子体素、PDE/生长审计和通过状态。实际矩阵为 `all_passed=false`，失败状态保留且不切换默认，完成 7.1；7.2 仍需统一覆盖/泄漏和最终质量门禁。

2026-09-06 64³ 质量门禁矩阵：扩展 `build_volume_acceptance_matrix.py`，逐 run 输出跨分区、越域、实体穿透、PDE 收敛、覆盖率和泄漏字段及布尔门禁；缺少证据或失败明确为 false。矩阵实际未通过，但检查结果完整落盘，完成 7.2。

2026-09-06 样例验收报告：新增 `docs/VOLUME_PARTITION_ACCEPTANCE_REPORT.md`，汇总 front/四视图/提升消融/根连接的实际 run、拒绝原因和解锁条件；明确 64³ 三组及 128³/10k 尚未通过，并记录 GitNexus `detect_changes` 风险为 LOW，完成 7.4。

2026-09-06 分区方向/PDE 接线：`lib/multiview_pde.py` 对 side 观测按 front/最近硬边界进行轴向符号一致化，使用分区内最近源初始化，并通过 `label_partition_components`、逐组件锚点检查和 `require_component_convergence` 拒绝不可解组件；方向符号和组件残差回归测试通过，完成 4.2。

2026-09-06 根/guide/RK4 接线：根连接计划与 `root_export` 统一 root label，`select_partitioned_guide_indices` 拒绝跨分区 guide，RK4 阶段使用分区标签体积和整线段守卫；相关方向、最近源、根连接和 RK4 分区测试共 31 项通过，完成 5.1。

2026-09-06 人工曲线局部重求解：`lib/manual_curve_constraints.py` 提供多视角曲线可见性/分区拒绝、冲突原因和自动路径保持；`lib/local_pde_resolve.py` 提供 halo 局部问题提取与非边界受影响体素合并。相关测试覆盖遮挡、跨区、空区域和边界保护，完成 13.2。

2026-09-06 补生长规划：新增 `lib/supplemental_growth.py`，组合可见覆盖缺口补根、同区 guide 门禁、停止原因和固定种子随机增根基线，输出覆盖增益对照并保留已有根计数。新增规划回归测试，完成 11.3。

2026-09-06 加权分区传播：`lib/recon_strategy/volume_partition.py::propagate_partitions` 已使用物理 spacing 和证据置信度构造域内图最短路，保留最佳/次佳代价生成置信度，并对硬阻隔、未播种组件和未分配体素拒绝；`tests/test_volume_partition_integration.py` 覆盖硬界面和无锚组件，完成 3.4。
