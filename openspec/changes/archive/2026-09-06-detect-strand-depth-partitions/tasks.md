## 1. 隔离检测实现

- [x] 1.1 在 `scripts/vis/` 新增方向双角场、mask-aware 深度跳变和稳健融合分数实现
- [x] 1.2 实现多阈值内部边缘清理、阻隔连通分区和面积统计
- [x] 1.3 输出基于 `raw_img.png` 的方向、深度、融合边缘及分区对照图和 JSON/NPZ 报告

## 2. 验证

- [x] 2.1 为方向符号不变性、真实角度跳变、外轮廓抑制和单区回退增加测试
- [x] 2.2 通过 `pixi` 运行针对性测试和当前样例正面审计
- [x] 2.3 检查检测结果不依赖发缝 mask，并记录对已有发缝区域的事后精度/召回率

## 3. 人工门禁与后续接入

- [x] 3.1 停止在二维边缘/分区可视化，等待用户确认是否允许进入分区 PDE 实现
- [x] 3.2 人工通过后，对 PDE 求解器、多视图策略和入口执行 GitNexus 影响分析并确认风险为 LOW

## 4. 分区 PDE

- [x] 4.1 为 weighted screened-Poisson 增加分区标签门控，跨标签邻接扩散权重严格置零
- [x] 4.2 将 front 二维标签提升为三维体素标签，并通过显式 CLI 参数接入多视图 PDE
- [x] 4.3 增加跨区不扩散、无锚分区拒绝和二维到三维提升回归测试
- [x] 4.4 在当前样例运行分区 PDE，并停下检查方向场和发丝是否仍发生跨区
