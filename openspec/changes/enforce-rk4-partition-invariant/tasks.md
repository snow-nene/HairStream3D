## 1. 变更前检查

- [x] 1.1 对 RK4 合成入口、guide 匹配和诊断输出执行影响分析，并记录当前 7.82% 跨区基线

## 2. 分区不变量实现

- [x] 2.1 实现离散标签最近邻查询、同标签方向归一化插值和跨区线段回退原语
- [x] 2.2 将根标签、四次标签感知查询、最终候选门禁和分区诊断接入 `hair_synthesis_rk4`
- [x] 2.3 将三维标签体接入 guide/full 两次积分，并限制普通发丝只匹配同标签 guide

## 3. 验证

- [x] 3.1 增加标签插值、跨区回退、无效根标签、同区 guide 和默认兼容回归测试
- [x] 3.2 通过 pixi 运行针对性测试，并确认未启用分区参数时现有路径不变
- [ ] 3.3 在当前样例运行 64³ 分区 smoke，输出最终跨区率并停下等待人工检查
